{
  "name": "wms",
  "version": "1.0.0",
  "description": "any",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd wms-admin && yarn install",
    "dev": "cd wms-admin && yarn dev",
    "build": "cd wms-admin && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.5.1"
  }
}
